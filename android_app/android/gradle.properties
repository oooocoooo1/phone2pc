org.gradle.jvmargs=-Xmx4G
org.gradle.parallel=true
org.gradle.caching=true
android.useAndroidX=true
# All current dependencies publish AndroidX artifacts. Avoid the legacy
# Jetifier transform, which wastes build time and can exhaust CI heap space.
android.enableJetifier=false
