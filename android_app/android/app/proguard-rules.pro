# Project-specific R8 rules belong in this file. Flutter and Android plugin
# consumer rules are applied automatically; keep this entry point present so
# release builds don't reference a missing configuration file.
